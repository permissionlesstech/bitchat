# iOS
platform :ios, '16.0'
target 'bitchat_iOS' do
  use_frameworks! # prefer dynamic; avoids some xcframework quirks
  pod 'TorManager', '~> 0.4'
  pod 'Tor/GeoIP', '~> 408.17'   # force a newer Tor with sim slices
end
